<h1>User info</h1>
<%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%></br>
<table class="table">
  <tbody>
    <tr></tr>
    <tr>
      <th>name</th>
      <th><%= @user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= @user.introduction %></th>
    </tr>
    <tr>
      <%= link_to "here", edit_user_path %>
    </tr>

  </tbody>
</table>


<h1>New book</h1>
<%= form_with model:@bookn, local:true do |f| %>

  <h4>Title</h4><%= f.text_field :title %>
  <h4>Opnion</h4><%= f.text_area :body %>
<br>
  <%= f.submit 'Create book' %>
  <% end %>

<H1>Book detail</H1>
<table class="table table-hover table-inverse">
  <tbody>
    <tr>
    <td>名前</td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <td><%= link_to 'Edit',  edit_book_path %></td>
    <td><%= link_to 'Destroy', book_path(@book.user_id), method: :delete, data: { confirm: '本当に削除しますか?' } %></td>
    </tr>
  </tbody>
</table>